

1) Dry: Do not repeat yourself
2) vector is also a generic
3) we can  also use structs with generics
4) Use generics with enums
5) can also use in methods (in impl)
6) traits are rules which type can do

7) Type must support comparison.