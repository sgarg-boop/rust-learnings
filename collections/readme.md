
1) vector (dynamic array)
2) strings (growable text)
3) hashmaps (key-value)

4) unlinke array and tuples (size already known)
5)these three things are stored in heap, unknown at compile time




strings desc
1) there are two types of encoding
    ascii
    utf-8

2) in rust strings are utf-8 encoded
3) in ascii: each has is of one byte -> only eng chars
4) in utf-8 every char has its own bytes -> anything we can store chinese, hindi etc.
5) read baout grapheme clusters and all


hashmaps desc
1)
